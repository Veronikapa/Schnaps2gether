<resources>
    <!-- Default screen margins, per the Android Design guidelines. -->
    <dimen name="activity_horizontal_margin">16dp</dimen>
    <dimen name="activity_vertical_margin">16dp</dimen>
        <dimen name="txt">30dp</dimen>

    <!--Dimensionen für Karten -->
    <dimen name="karten_d_normal">
    <item name="android:layout_width" >60dp</item>
    <item name="android:layout_height" >90dp</item>
    </dimen>

    <!--small -Werte x 0.75 -->
    <dimen name="karten_d_small">
        <item name="android:layout_width" >45dp</item>
        <item name="android:layout_height" >68dp</item>
    </dimen>

<!--large -Werte x 1.5 -->
    <dimen name="karten_d_large">
        <item name="android:layout_width" >90dp</item>
        <item name="android:layout_height" >135dp</item>
    </dimen>

    <!--xlarge -Werte x 2 -->
    <dimen name="karten_d_xlarge">
        <item name="android:layout_width" >120dp</item>
        <item name="android:layout_height" >180dp</item>
    </dimen>

    <!--Dimensionen für Stich -->
    <dimen name="stich_d_normal">
        <item name="android:layout_width" >35dp</item>
        <item name="android:layout_height" >60dp</item>
    </dimen>

    <!--small -Werte x 0.75 -->
    <dimen name="stich_d_small">
        <item name="android:layout_width" >35dp</item>
        <item name="android:layout_height" >60dp</item>
    </dimen>

    <!--large -Werte x 1.5 -->
    <dimen name="stich_d_large">
        <item name="android:layout_width" >53dp</item>
        <item name="android:layout_height" >90dp</item>
    </dimen>

    <!--large -Werte x 2 -->
    <dimen name="stich_d_xlarge">
        <item name="android:layout_width" >70dp</item>
        <item name="android:layout_height" >120dp</item>
    </dimen>

</resources>


